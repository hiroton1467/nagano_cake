<h3>商品一覧</h3>

<%= link_to "商品新規登録", new_admin_item_path %>

<table>
 <tbody>

<% @items.each do |item| %>

<tr>
 <td><%= item.id %></td>

 <td><%= link_to item.name, admin_item_path(item.id)%></td>

 <td><%= item.price %></td>

 <td><%= link_to "Edit", edit_admin_item_path(item.id) %></td>
 <td><%= link_to "Destroy", admin_item_path(item.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>

</tr>


<% end %>

 </tbody>
<ta>