<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
    <% if admin_signed_in? %>
      <p><%= link_to "ログアウト", destroy_admin_session_path, method: :delete %> </p>
      <p><%= link_to "顧客一覧", admin_customers_path %></p>
      <p><%= link_to "商品一覧", admin_items_path %></p>
      <p><%= link_to "注文履歴", admin_root_path %></p>

    <% elsif customer_signed_in? %>
      <p><%= link_to "ログアウト", destroy_customer_session_path, method: :delete %></p>
      <p><%= link_to "商品一覧", items_path %></p>
      <p><%= link_to "カート", cart_items_path(current_customer.id) %></p>
      <p><%= link_to "マイページ", customer_path(current_customer.id) %></p>

    <% else  %>
      <p><%= link_to "新規登録", new_customer_registration_path %></p>
      <p><%= link_to "ログイン", new_customer_session_path %></p>
      <p><%= link_to "商品一覧", items_path %></p>
      <p><%= link_to "About", homes_about_path %></p>
    <% end %>
  </header>
    <%= yield %>
  </body>
</html>
